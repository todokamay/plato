# Codex Agent Rules

Codex and all AI agents must read `AGENTS.md` before editing Plato.

Required defaults:

- Work only in `C:\Users\User\Documents\plato`.
- PONYTAIL MODE IS ALWAYS ON FOR CODEX AND ALL AI AGENTS.
- Do not modify, move, delete, or overwrite source MP4 files.
- Do not add cloud, LLM, OCR, Whisper, VLM, auto-publishing, or Resolve automation unless explicitly requested.
- Run `py tools\check_repo_safety.py` before committing.

